import { FilterTasksPipePipe } from './filter-tasks-pipe.pipe';

describe('FilterTasksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTasksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
